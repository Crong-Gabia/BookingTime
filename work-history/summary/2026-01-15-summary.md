# 2026-01-15 작업 요약

## 완료된 기능

### ✅ 응답 마감일(옵션) + 토스트 알림

**목표**: 회의 요청에 응답 마감일을 옵션으로 설정 가능하게 하고, 사용자 피드백(alert/confirm/prompt)을 토스트 알림으로 통일.

## 주요 변경사항

### 백엔드 (Backend)

1. **데이터베이스**
   - `MeetingRequest.responseDeadlineAt` (nullable) 추가
   - 마이그레이션: `20260114141246_add_response_deadline_at`

2. **DTO**
   - `CreateMeetingRequestDto.responseDeadlineAt?: string | null` 추가
   - `DashboardDto.responseDeadlineAt?: string | null` 추가

3. **API 로직**
   - `createRequest()`: 마감일 유효성 검사 후 저장
   - `getDashboard()`: 마감일 포함해서 응답
   - `createResponse()`: 마감일 경과 시 `REQUEST_CLOSED` 반환
   - `remindParticipant()`: 마감일 경과 시 `REQUEST_CLOSED` 반환
   - `updateOrganizerAvailability()`: 불필요한 `$transaction` 제거

### 프론트엔드 (Frontend)

1. **공통**
   - `notistack` 라이브러리 도입
   - `useToast` 커스텀 훅 추가 (`success`, `error`, `info`, `warning`)
   - `SnackbarProvider` 설정 (max 3개, 3초 autoHide)

2. **페이지별 변경**
   - **CreatePage**: 응답 마감 옵션 UI 추가 (없음/오늘18:00/내일18:00/직접입력)
   - **ResponsePage**: `alert()` → `toast.error()` 교체
   - **DashboardPage**: `alert()` → `toast.success/info/error()` 교체 + `handleCopyLink` async 처리
   - **OrganizerSlotSelectionPage**: `alert()` → `toast.error/success()` 교체

3. **테스트**
   - `vi.Mock` → `Mock` 타입 임포트 수정 (`type Mock` 추가)
   - `DashboardPage.test.tsx`: mock 데이터 구조 최신화 (`commonAvailableSlots`가 객체 배열로 변경)
   - 모든 테스트 통과

### 가이드라인 업데이트

- `guidelines/01-ui-components.md`: `alert/confirm/prompt` 사용 금지 + 토스트 통일 규칙 추가
- `guidelines/04-data-fetching.md`: 예제 코드에 토스트 사용으로 변경

### 개발 환경

- `localhost` → `127.0.0.1`: DNS 문제 회피를 위해 Vite dev server host 고정
- `apps/api/prisma/.env`: DATABASE_URL host를 `127.0.0.1`로 변경

## 검증 결과

- ✅ `pnpm --filter api build` - 성공
- ✅ `pnpm --filter api test` - 성공 (6/6 passed)
- ✅ `pnpm --filter web build` - 성공
- ✅ `pnpm --filter web test` - 성공 (148/148 passed)
- ✅ LSP diagnostics clean

## 생성된 PR

- **PR #13**: feat: 응답 마감일(옵션) + 토스트 알림
- **브랜치**: `feat/response-deadline-toast` → `develop`

## 기타

- 운영 규칙(`AGENTS.md`) 업데이트: 작업 시작/재개 시 이슈 확인 의무화 + PR 리뷰 대응 루틴 추가
